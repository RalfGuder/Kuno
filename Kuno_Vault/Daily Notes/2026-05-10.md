---
title: 2026-05-10
date: 2026-05-10
tags:
  - daily
  - kuno
  - c64
---

# 2026-05-10 -- Sonntag

> [!abstract] Vault-Geburtstag
> Erste Daily Note überhaupt. `Kuno_Vault` heute angelegt -- analog zu `iTWO_Vault` im RIB-Projekt -- als künftige Standard-Ablage für Notizen rund um das Kuno-Repo.

## Stand

**Branch:** `dev/c64`

Gestern (2026-05-09) wurden in 12 Commits die **C64-Sprite-Pipeline** komplett aufgesetzt:

- `tools/sprites/build_c64_sprites.py` -- Python-Build mit `pack_phase`, Config-Loader (Slot/Range/Duplicate-Validation), `build_bin`, `build_inc`, `render_preview`.
- `src/main/trse/Kuno/sprites/sprite_phases.json` -- **27 Phasen** (Kuno spawn/walk/jump/stand × LR, Slimer, Gecko, Wizrot, ...) ab Slot 200.
- `kuno_sprites.bin` + `kuno_sprites.inc` (TRSE-`@define`) werden generiert; `main.ras` ist auf `kuno_sprites.bin` umgestellt, TRSE-Samples raus.
- letzter Commit: `25b07883 Replace em-dash with double-hyphen in .inc header for TRSE parser` -- der TRSE-Parser stolpert über Unicode-`--` im Header-Kommentar.

Siehe [[Specs/C64 Sprite Pipeline]] (folgt).

## Uncommitted heute

```
M cpp/MAINFILE.CPP        (+/- ~112 Zeilen)
M cpp/SPIEL.CPP           (+/- ~172 Zeilen)
M cpp/SPRITEDA.CPP        (+/- ~14 Zeilen)
M img/KBEGINN1.TGA, KBEGINN3.TGA, KBEGINN4.TGA   (1290 → 1290 Bytes, also Inhalts-/Palette-Edit)
M src/main/trse/Kuno/sprites/kuno_sprites.inc    (Header-Kommentar)
M tools/sprites/build_c64_sprites.py             (Header-Kommentar entsprechend)
```

## Beobachtungen

> [!warning] Encoding-Konflikt mit CLAUDE.md
> Der Diff in `cpp/MAINFILE.CPP`, `cpp/SPIEL.CPP`, `cpp/SPRITEDA.CPP` zeigt eine **systematische Reparatur** kaputter Umlaute: `enth�lt` → `enthält`, `Quellcode fr` → `Quellcode für`, `L„dt` → `Lädt`. Zusätzlich werden Tabs zu Spaces expandiert.
>
> Die `CLAUDE.md` warnt aber explizit: *"Encoding der C++-Dateien: ... CP437 (DOS) -- beim Editieren das Encoding nicht auf UTF-8 umstellen, sonst werden Kommentare zerstört."*
>
> Hier scheint das Gegenteil zu passieren -- die ohnehin schon kaputten CP437-Reste werden zu sauberem UTF-8 normalisiert. Falls das gewollt ist, sollte die `CLAUDE.md`-Regel gelockert oder umformuliert werden.

## Offene Fragen

- Soll die `CLAUDE.md`-Encoding-Regel angepasst werden (CP437 → UTF-8 ist offenbar erwünscht)?
- Sollen die TGA-Edits in `img/KBEGINN[134].TGA` zusammen mit den cpp-Änderungen committet werden, oder separat?
- Nächster Schritt nach Sprite-Pipeline: tatsächliche Bewegung/Animation in `main.ras`, oder erst Charset/Level?

## Nächste Schritte

- [ ] Encoding-Repair-Commit für `cpp/*.CPP` mit klarer Message
- [ ] `Specs/C64 Sprite Pipeline.md` mit Architektur-Skizze, Phasen-Tabelle und Test-Workflow nachziehen
- [ ] Entscheiden, ob `CLAUDE.md` angepasst wird

## Verweise

- [[README|Vault-Root]]
- Repo-Pfade: `tools/sprites/build_c64_sprites.py`, `src/main/trse/Kuno/Kuno.trse`
